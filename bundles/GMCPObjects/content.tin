#event {IAC SB GMCP objects.nums IAC SE}
{
    #foreach {$bundle_handler_objects_nums[]} {bhon} {
        $bundle_handler_objects_nums[$bhon] %0;
    };
}

#event {IAC SB GMCP objects.data IAC SE}
{
    #foreach {$bundle_handler_objects_data[]} {bhod} {
        $bundle_handler_objects_data[$bhod] %0;
    };
}