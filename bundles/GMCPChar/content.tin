#event {IAC SB GMCP char.info IAC SE}
{
    #foreach {$bundle_handler_char_info[]} {bhon} {
        $bundle_handler_char_info[$bhon] %0;
    };
}

#event {IAC SB GMCP char.state IAC SE}
{
    #foreach {$bundle_handler_char_state[]} {bhod} {
        $bundle_handler_char_state[$bhod] %0;
    };
}